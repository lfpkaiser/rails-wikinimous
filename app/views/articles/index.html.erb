<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
  <li><%=link_to article.title, article_path(article)%></li>
  <p style="max-width: 100px word-wrap: break-word;"><%= article.content %></p>
  <% end %>
</ul>

<%= link_to "Create new article", new_article_path%>
